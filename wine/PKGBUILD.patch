--- a/PKGBUILD	2017-02-26 19:56:57.000000000 +0100
+++ b/PKGBUILD	2017-02-26 20:02:34.361048094 +0100
@@ -11,10 +11,10 @@
 
 _pkgbasever=${pkgver/rc/-rc}
 
-source=("https://github.com/wine-compholio/wine-patched/archive/staging-$_pkgbasever.tar.gz"
+source=("wine-staging-2.2.tar.xz"
         30-win32-aliases.conf)
-sha1sums=('b69507f19d1f543b52422d2fd34e0350e070423d'
-          '023a5c901c6a091c56e76b6a62d141d87cce9fdb')
+sha1sums=(SKIP
+          SKIP)
 
 pkgdesc="A compatibility layer for running Windows programs - Staging branch"
 url="http://www.wine-staging.com"
@@ -108,7 +108,7 @@
 
 prepare() {
   # Allow ccache to work
-  mv wine-patched-staging-$_pkgbasever $pkgname
+  mv wine-staging-$_pkgbasever $pkgname
 
   sed 's|OpenCL/opencl.h|CL/opencl.h|g' -i $pkgname/configure*
 
@@ -118,6 +118,12 @@
 }
 
 build() {
+
+  cpucount=$(( $(cat /proc/cpuinfo|grep processor|tail -1|awk '{print $3}') + 1 ))
+
+  echo "CPU count: $cpucount"
+  sleep 1
+
   cd "$srcdir"
 
   if [[ $CARCH == x86_64 ]]; then
@@ -133,7 +139,7 @@
       --enable-win64 \
       --with-xattr
 
-    make
+    make -j $cpucount
 
     _wine32opts=(
       --libdir=/usr/lib32
@@ -152,7 +158,7 @@
     --with-xattr \
     "${_wine32opts[@]}"
 
-  make
+  make -j $cpucount
 }
 
 package() {
